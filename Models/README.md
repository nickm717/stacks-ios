# Models

Empty on purpose. Real models land with **STK-5** (schema migration from data
model) and should mirror `stacks-context/engineering/data-model.md` —
particularly the editions-vs-copies split. Don't invent model shapes here
ahead of that spec.
