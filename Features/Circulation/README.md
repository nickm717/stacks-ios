# Features/Circulation

Empty on purpose. This is a shared subsystem — read every spec that
references the copy-state model before touching it. Start at
`stacks-context/circulation/copy-state-model.md`.
